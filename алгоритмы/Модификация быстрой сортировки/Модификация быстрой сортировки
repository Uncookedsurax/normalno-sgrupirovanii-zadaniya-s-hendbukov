#include <iostream>
#include <vector>
#include <algorithm>

void sort(std::vector<int>v) {
	sort(v.begin(), v.end());

	for (auto num : v) {
		std::cout << num<<" ";
	}
}

int main(){
	int n;
	std::cin >> n;
	std::vector<int> a(n);
	for (int i = 0; i <= n-1; ++i){
		std::cin >> a[i];
	}
	sort(a);
}
